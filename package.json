{
  "name": "@livemoe/monorepo",
  "packageManager": "pnpm@7.1.0",
  "version": "0.0.1",
  "private": true,
  "description": "",
  "keywords": [],
  "license": "MIT",
  "author": "Seven",
  "repository": {
    "type": "git",
    "url": "git+"
  },
  "scripts": {
    "build": "rimraf packages/*/dist packages/*/build && pnpm -r build",
    "stub": "pnpm -r --parallel run stub",
    "release": "bumpp package.json packages/*/package.json --commit --push --tag",
    "test": "vitest"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^0.23.1",
    "bumpp": "^7.1.1",
    "eslint": "^8.15.0",
    "lint-staged": "^12.4.1",
    "pnpm": "^7.1.0",
    "rimraf": "^3.0.2",
    "simple-git-hooks": "^2.7.0",
    "typescript": "^4.6.4",
    "unbuild": "^0.7.4",
    "vitest": "^0.12.6"
  },
  "lint-staged": {
    "*.{js,ts}": [
      "eslint --fix"
    ]
  },
  "simple-git-hooks": {
    "pre-commit": "npx lint-staged"
  }
}
